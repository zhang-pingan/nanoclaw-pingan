# Dynamic Workflow Contract Pack

This directory is the machine-readable authority for Dynamic Workflow Runtime contracts. G0.2 provides the common artifact envelope, exact `VersionedRef`, strict JSON, canonical/domain-separated SHA-256, foundation fixtures, and the conformance entry point. G0.3 adds closed Definition, Recipe, Runtime Command, Transition, Feature Manifest vNext, Card Presentation, Source IR, and Compiled IR schemas with TypeScript key/union conformance and positive/negative fixtures. G0.4 adds closed Compiler Error, Runtime Fact/Event, Permission, Command Reason/Denial catalogs; runtime state transition tables; the command authorization table; and the declarative T0-T8/T6e transaction protocol table. G0.5 adds the immutable `local_single_user_safety@1`, closed hot-reload Capacity baseline, Product Floor, Retention policy, complete per-field Enforcement Matrix, and the explicitly uncertified `local_single_user_sqlite@1` candidate. G0.6 adds the canonical metadata source for all 74 Normative Logical Schema objects, complete expanded column metadata, internal typed relations, explicit external-reference validators, CHECK/UK/FK/index intent, and the fixed query intent catalog. G0.7 adds the machine-generated `WorkflowRuntimeAbsenceBaseline`, `ProductSurfaceCoverageManifest`, and `MigrationCandidateBoundaryManifest`. G0.8-G0.10 and R-016 remain construction provenance from the original gate sequence. The G2 semantic-correction Contract, per-case inputs, actual Compiler candidate, and working review bundle remain the exact, non-publishable `WORKING` source set. The explicit `prepare-rc` entry freezes those four roots into the single `conformance/review-candidate/g2-semantic-correction/` Review Candidate. The RC is `RC_REVIEW`, non-publishable, Production-unreachable, and invalidated by any bound source/toolchain/compiler/Working identity drift. It does not create a Draft v5/v6 or an additive RC chain. Draft v3/v4 review history is retained only by Git commit history and is absent from the active dependency graph; no current Contract/check path reads `.git` or invokes Git history.

`safety/` contains G0.5 contracts. `sqlite/` contains the G0.5 SQLite candidate plus G0.6 logical metadata artifacts. `static/` contains the three G0.7 closed schemas and generated manifests. The SQLite candidate has null release/native/SQLite observation fields and is not certification evidence. G0.6 records logical type/nullability/relation/constraint/index/query intent only: it contains no SQL text, executable migration, executable Schema Manifest, Supported Limits, Store, connection factory, SQLite open, or PRAGMA execution. G0.7 performs absence-only SQLite schema inspection and does not execute Runtime SQL or migrations. Historical G0/R-016/Draft artifacts remain available for explicit construction archive verification. `conformance/sealed/g2-semantic-correction/` remains the immutable 3.0.0 predecessor oracle and its independent replay remains 29/40. The additive `conformance/sealed/g2-production-compiler-replay-repair-v2/` successor remains the immutable owner-approved 3.0.1 predecessor at bundle `sha256:d99647d8ca6aabc737a793019335e6770aa111a79be7545c4dec00c6e7af2145`. The current `conformance/sealed/g2-capability-outbox-binding-v3/` successor binds Compiler 3.0.2, the exact Capability Outbox execution binding, closed Plan/result schemas, owner-approved Draft/report/GoldenSemanticReview, and 157 sealed artifacts at bundle `sha256:b3ed9e43bd0fadaf40520257926dcf690ee8495bb417220245f248385bde9efb` with artifact hash `sha256:967437bb9f91e32e5014b2af90a23f5646e491eb427bdf55accb345ead70db8f`; current independent replay is 40/40 exact. All three bundles are non-publishable and Production-unreachable; no G5 Runtime is created by G2 seal or replay commands.

The G1 Publisher idempotency/audit Schema prerequisite is the G1-owned additive physical input `src/workflow-runtime/store/schema/inputs/workflow-publisher-schema-prerequisite@1.json`, artifact `sha256:93ca47fc61812061c77400b78c2285790a86ea852e1358d5369f95db55ba4330`. It preserves the exact G0.6/G0.10 artifacts and historically advanced the executable Database Schema to `2`, with 81 tables. `workflow_publisher_commands`, `workflow_publisher_command_invocations`, and `workflow_publisher_events` provide caller idempotency, schema-bound request/source/plan/receipt/result/detail Values, typed Registry/Closure/Feature Release FKs, approved review actor/session/expiry, durable disposition, adjacent immutable invocation/event hash chains, partial recovery lookup, and deterministic query/constraint fixtures. Existing Registry/Release/Retention/Value tables remain authoritative.

G1.5 adds `src/workflow-runtime/store/schema/inputs/workflow-feature-release-activation-schema-prerequisite@1.json`, artifact `sha256:2e2e98cd8276d3b42b796afb34508be44800619d3781ddd88f10022c55a7a46e`, and preserves the reproducible Database Schema `3` construction input with migration hash `sha256:eea3547a0f5208d08bfbe771de3895bba020ca3cf34ddf2fb4e3b7945765d345`. Its Release, active-pointer, and Retention extensions continue to enforce single-active lifecycle, row-version CAS, target-active, identity/delete protection, held observations, and active/draining retention protection.

G1.6 adds the independent physical input `src/workflow-runtime/store/schema/inputs/workflow-feature-release-activation-failure-replay-schema-prerequisite@1.json`, artifact `sha256:d1fd091aedae1d8d6d51e3442c074050ab57ab5330e18bf37adef628b8dcba9d`, and historically advanced the executable Database Schema to `4`. It consumes the frozen G3.8A 62-column, 12-composite-FK, 7-UK handoff; rebuilds Activation command/invocation/event around canonical caller request Values, nullable verified prefixes, schema-bound canonical terminal results and immutable Invocation bindings; and enforces applied-only receipt, closed exact replay/domain drift disposition, Event replay/conflict types, and all eight query plans. Historical G1 root/schema/migration are `sha256:6f49451868b7a5cab359d1c21f14f79afbc11b12aa1938039daf5914d9c4d591` / `sha256:f517a5e7bb8b3ea91bb37cd6a68b32898ceb62b9044687a8103808be6852106a` / `sha256:4a8ddeb1f9715399ad96c3bc32efa5e8032a3bd484eaed0159c6a24620c1be43`; Schema 3-to-4 upgrade is `sha256:5ac263fe3279c61f74ba6314f5df98fff59a8f8b32acfa784d2040421ebaa3cf`; Schema 4 SQLite identity is `sha256:e46f58e49b42ad53e3d744de86b6d8fb6299236258459c35d9ca3affa440932c`. These artifacts remain frozen provenance. The 3-to-4 upgrade is allowed only when the three Activation relations and active-pointer relation are empty; Store validates frozen Schema 3 identity before DDL and rolls back every failure.

G1.7 is the current Capacity Admin CAP1 Invocation persistence repair. Database Schema `5` adds immutable `prepared` only for the first authenticated, authorized, exact-request CAP1 Invocation with `decided_at_ms >= requested_at_ms`, null denial, and null `applied_at_ms`; CAP2-CAP4 never update that row, CAP4 finalizes only the Command and `watcher_published` Event, and a later exact retry appends `duplicate`. The 4-to-5 upgrade copies before installing Schema 5 INSERT guards and preserves every Schema 4-valid `applied | denied | conflict | duplicate | failed` row byte-for-byte. Current INSERT guards reject new `applied`, constrain prepared/terminal decision and denial shapes, and require duplicates to reference the exact finalized Command result. Current G1 root/schema/dependency/physical identities are `sha256:baa39d55cac34133a29b461466aa450fec59bd2fd6df72334e8b33d1d1619869` / `sha256:49aaee7c8f046cd9a15b3bc5b77fbcf1713be2a1872078941043f5ccdca29024` / `sha256:d08cfaae72c003b11a05cb1fbfa546f7cce7fad9ecb56d0746f33de294b8088c` / `sha256:ba025b32bb028f2ffe5df45d9440cd0a897e0a06c076b10b6f641c265ae02090`. Fresh migration, Schema 4-to-5 upgrade, Schema Manifest artifact, and SQLite identity are `sha256:2ead40dc2f1618f87247e9d3bb476266797c38560e1ad0537a6afa6f71a3fbf6` / `sha256:97479810c2c079d71270d5a714faa4b8fa8ebd6af629ef2f7d772af270c2bb0a` / `sha256:c9bce166112023cf5e09d41901938f74efbc69cff36da9428a3c21c3064d8439` / `sha256:5ee3c119cc6a0e0552e2a6fe45b51c8ffd08ec7acdbac66748978ed0d21fdb0a`. The `37a9792` candidate identities remain provenance in the implementation ledger. This is persistence/Contract prerequisite only: no Capacity Gateway/Publisher/Watcher, Runtime business DML, or G5-G9 implementation exists.

G3.1 adds only the read-only Registry publish-preflight foundation. `registry/` contains closed Draft 2020-12 input, result, and foundation schemas plus the exact G3.1 foundation artifact. `conformance/g3-registry-publish-foundation/` contains three explicitly test-only positive cases and 23 negative cases, including missing/mismatched/latest/test-only Capability Outbox bindings. The current exit-candidate pack is `icarus.workflow-contract-pack-g3-registry-publish-foundation@1.0.0`, `sha256:aae22ae7f5ea1fa06a5c9b6c63eeadbb61b32d3510f503754ef5d8617cab0227`. It accepts a zero-Recipe Production Registry, rejects test-only promotion, moving refs, identity/hash/ABI/closure drift and side-effect requests, requires Capability bindings to exact `outbox_adapter` and `outbox_policy` closure members, and pins the exact current G1/G2/compiler/schema/retention identities. It does not implement Registry persistence, a Production loader, Feature/Core Release, Authoring stages, review approval, Publisher execution, Publish, Activation, or G4-G9.

G3.2A freezes the missing Feature Manifest vNext strict-intake semantics without implementing the reader. `registry/` contains the closed Draft 2020-12 strict-intake profile and result schemas plus the profile, domain catalog, fixture artifacts, and pack. `conformance/g3.2a-feature-manifest-intake/` is the isolated test-only fixture root with 2 positive and 18 negative cases. The pack is `icarus.workflow-contract-pack-g3-2a-feature-manifest-intake@1.0.0`, currently `sha256:c9c273b6d294d512a3578203d91d4bdce7863a3ccb561fdd7da08d072b3d8cd9`; profile `sha256:643f6907c7723b8c77aa2a1b1dc51fcf5dac2e16404d2cd862909f43f7c75de4`, profile schema `sha256:ead0a25980c695ecc4564044f98359cdefb76780b959f2a98361a0b5d0c0b614`, result schema `sha256:95e444fbaf0c64c4a89c2b1ee6aa348a11ace85fbd57e8a3d1b63a16416ef8f3`, positive fixtures `sha256:b8280197763f6741e69540748a943d5975f0c0999ecbca04b2ceee484b7cf39f`, negative fixtures `sha256:77ced4c1ba39632c2e662aa247593c68fac96e6068832abb4081fbf8e9030be1`. It freezes `<featureId>.feature` derivation, `namespace`/`registry_namespace` equality, `<featureId>.<localId>` resource ownership, exact cross-owner dependency pins, canonical source roots, `source_path` relative to `feature_source_root` and below `workflow-src`, symlink/hard-link/moving-root rejection with device/inode snapshot and TOCTOU checks, the ASCII `icarus:feature-manifest-source:2\n` hash domain, JCS/UTF-8 input and manifest-hash exclusion, all three ASCII tuple orderings and unique identities, and the single-diagnostic phase precedence. The evaluator never reads resource `source_path`, writes Registry state, executes Publisher/Publish, or activates a release; `reader_invoked` and `resolver_invoked` are always false. G3.2A remains immutable and G3.2 implementation is now `DONE`; G3 remains `IN_PROGRESS` and G4-G9 remain `NOT_READY`.

G3.2 Strict Intake Preflight implements only the read-only execution of that frozen contract. `g3-2-feature-manifest-intake.ts` first delegates bytes/structural/schema/hash/ownership/order/path decisions to the G3.2A evaluator, then resolves the canonical feature root, performs `lstat`/`O_NOFOLLOW`/`fstat` identity checks before and after every declared source read, rejects root/path symlinks, hard-link reuse, missing or moving paths, verifies raw source-byte SHA-256 values, and invokes an explicit exact release/resource dependency resolver. It never reads legacy resource keys, writes Registry state, publishes, activates, or loads Production resources. The separate pack is `icarus.workflow-contract-pack-g3-2-feature-manifest-intake@1.0.0`, currently `sha256:1eb0b81f488f4a37fa4503ddfef0dfa8a56d40fdeb535c9758d9d21fd39bb92b`; the profile/result schemas and fixture hashes are recorded in `contract-pack-g3.2-feature-manifest-intake.json`. Use `./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.2:check` and `./scripts/runtime-toolchain.sh exec -- npm run test:g3`.

G3.3 adds the first Registry persistence boundary without opening Publish. `g3-registry-persistence-types.ts` and `g3-registry-persistence.ts` define closed resource, dependency-closure manifest, snapshot, and snapshot-preflight input/result contracts. The pack is `icarus.workflow-contract-pack-g3-registry-persistence@1.0.0`, currently `sha256:ff5d40589df9bc78e2e7f4f0e2fdcd5f0a7b7f34e0d0592e5308c1230425f1b3`; its isolated `conformance/g3-registry-persistence/` root contains one exact positive batch and four deterministic identity/dependency/closure/snapshot negatives. Resource content hashes use `icarus:workflow-registry-resource-content:1\n`; closure hashes reuse the frozen G2 `icarus:workflow-registry-dependency-closure:1\n` payload `{format, root_resource_type, root_ref, members, member_count}` and unsigned ASCII tuple ordering; the only dependency kind is `registry_exact`. `src/workflow-runtime/store/registry-persistence.ts` writes only staged resources through the existing synchronous Store transaction, uses inline canonical Values and existing deferred FKs, and derives internal row IDs from immutable refs. Its read-only snapshot preflight canonicalizes the closure Value and every member Resource Value, verifies exact transitive dependency membership/order/hash and snapshot compiler/Core/G1 schema bindings, and returns `read_only=true`. It does not read legacy/source paths or implement Publisher, Publish, Release, Retention/GC, Production loader, or Activation. Use `./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.registry:check` and `./scripts/runtime-toolchain.sh exec -- npm run test:g3`.

G3.4 adds immutable replay/collision handling to that same staged Store boundary without changing the G3.3 Contract Pack identity. Before DML, the existing synchronous transaction queries exact canonical Value metadata, resource ref/hash/owner/state, dependency sets, closure ref/hash/members, and snapshot ref/hash/bindings. Missing identities are inserted, exact identities are reused, a fully exact replay returns `disposition=exact_replay` with no DML, and any drift throws a stable `registry_*_identity_collision` or set-collision error with the transaction unchanged. The first committed `created_at_ms` is retained as installation metadata. No G1 DDL/schema, Publisher, Publish, Release, Retention/GC, Production loader, or Activation surface is added.

G3.5 adds the isolated exact Registry resource read/query preflight. Its closed input requires explicit `resource_type + VersionedRef + content_hash` plus exact schema binding, owner, publication state, and complete ordered `registry_exact` dependencies; moving/range refs, alias/fallback fields, active pointers, and launchability inference are rejected. `src/workflow-runtime/store/registry-resource-query.ts` uses only the existing Store read/query surface and verifies the canonical inline Value, resource content hash, exact schema resource/Value, owner columns, state, dependency rows, and target hashes in the frozen error precedence. Accepted results return verified canonical content; every result is closed and `read_only=true`. The pack is `icarus.workflow-contract-pack-g3-registry-exact-resource-query@1.0.0`, currently `sha256:04506acde71f4c03e081f310265df521515327caee37cc820c56fa2162ad4ffb`. It does not change the G3.3 behavior or generic Store boundary and adds no Publisher transaction, Publish, Release, Retention/GC, Production loader, Activation, or G4-G9 surface.

G3.6 adds the final standalone read-only Retention / Executor ABI compatibility preflight. Its closed input binds exact Feature Release, Snapshot, Closure root/member set, every typed Execution Artifact and Executor member, Core Compatibility, G0.4 Run Protocol, frozen Executor ABI v1, current G1 Database Schema `5`, and `local_single_user_retention@1` identities. `src/workflow-runtime/store/retention-executor-abi-preflight.ts` composes G3.3 `preflightRegistrySnapshot` and G3.5 `queryExactRegistryResource`; it adds no Registry SQL semantics. It verifies Closure completeness, Artifact/Executor ref/hash/entry/provider binding, major `1`, Core/DB bindings, and exact `published -> feature_release` Retention eligibility. Results are closed, deterministic, and `read_only=true`; no Retention Handle, GC/delete, publication state, Release, Publisher transaction, loader, or Activation is created. After the execution-binding cascade, the current pack is `icarus.workflow-contract-pack-g3-retention-executor-abi-preflight@1.0.0`, `sha256:4dd52b4f38da315ba0194fd23a9c681ffdba6aac37291100dd6d6b944178acd9`.

The bounded G3.6 audit historically found `PUBLISHER_BLOCKED_BY_SCHEMA`; the additive G1 prerequisite closed that blocker and the current G3.6 pack records `PUBLISHER_SCHEMA_PREREQUISITE_READY`. Workflow Runtime Command and Capacity Command tables remain closed and are not reused. G3 remains `IN_PROGRESS`; G3.7 now consumes this prerequisite for staged Publish, while Activation, loader, GC, and G4-G9 remain absent.

G3.7 implements the `WorkflowPublisher` staged publish vertical slice and no activation surface. `g3-workflow-publisher-types.ts` and the three published closed schemas define the canonical request, approved review, receipt, and invocation result; request/review/release/receipt/result/invocation/event identities each use an explicit domain-separated hash. The request binds caller `idempotency_domain + idempotency_key`, authenticated reviewer actor/session and expiry, exact source manifest/Compiled Plan Values, the G3.1 publish preflight, the complete G3.5 release-resource query set, the G3.6 compatibility/Retention preflight, and one exact target Feature Release. `authoring/workflow-publisher.ts` calls the existing preflights, with G3.6 continuing to compose G3.3 and G3.5, then uses the unchanged generic `withImmediateTransaction` host for one `BEGIN IMMEDIATE`: canonical Values, Publisher command/invocation/event audit, Registry `staged -> published` CAS, staged Feature Release and exact resource set, held published Retention root/member set, canonical receipt, and terminal command are atomic. Exact replay appends `duplicate`, same-key drift appends `conflict`, preflight rejection with complete typed identities records `failed`, and every stream remains adjacent, append-only, and hash-chained. The current pack is `icarus.workflow-contract-pack-g3-workflow-publisher@1.0.0`, `sha256:e80038f09ad841de630d961f137f15a2de14a487a74afb6b1d8b36edea689ba0`, rebuilt only because its exact G2/G3.6 input identities changed. Real-file SQLite tests cover five rollback points, post-commit reopen recovery, release collision, exact replay/conflict, durable failure, approval expiry, hash-chain ordering, and independent-database semantic determinism. No active pointer, Activation, Production loader, Artifact build/install, authoring compile, GC/delete, Runtime Command reuse, Capacity Command reuse, or G4-G9 surface is added; G3 remains `IN_PROGRESS`.

G3.8 is a persistence-readiness audit only; its historical result was `ACTIVATION_BLOCKED_BY_SCHEMA`, not `BLOCKED_BY_SPEC`. G1.5 closed the originally enumerated prerequisite with Database Schema `3`, the three Activation audit tables, owner-consistent Release/pointer and held-Retention constraints, and idempotency/history/recovery/CAS/preflight query intents. The attempted G3.9 mapping then proved that Schema 3 still cannot represent durable failed/conflict replay: failed lifecycle forbids a canonical receipt while the old prose generalized every exact replay as duplicate + receipt; the command header has no schema-bound canonical terminal result/Invocation identity; the Retention observation insert trigger aborts before a failed audit; and required caller-claim owner Release FKs likewise reject owner mismatch. No implementation-layer workaround is authorized.

G3.8A freezes the independent repair Contract Pack `icarus.workflow-contract-pack-g3-8a-activation-contract-repair@1.0.0`, currently `sha256:d8412111a0f3dcabb4ce416b99086701ea3e3911ff431b5457eb957b2f69722f`. Its versioned repair artifact is `sha256:94cb2c390bb44298238b1ffac4184b04f59efbbeae6f268fbce7618104ec406b` with internal contract hash `sha256:70d4b9ef47c83711415636737292450538acaf5cc4547d3130b04b101e6707ae`. Its recorded `status=SCHEMA_REPAIR_REQUIRED`, `production_reachable=false`, current Schema `3`, required Schema `4`, and `G3.9=BLOCKED_BY_G1_6` are frozen handoff facts; after G1.6 consumption the current project gate is `G3.9=READY`, without rewriting this artifact. The repair, scenario, and negative-case schemas are closed Draft 2020-12 schemas; 9 positive, 59 negative (12 structural/semantic mutation + 47 Activation/G3.6 error-precedence), and 17 fault fixtures freeze first applied/failed/pointer-conflict, exact replay, repeated domain drift, nullable receipt, every rejection classification/verified-fact prefix, pre-commit rollback, post-commit reopen, pending recovery, and command/result/receipt/Invocation/Event tamper. The Schema 4 handoff additionally publishes 62 exact column, 12 composite FK, and 7 UK requirements with SQLite type/nullability/enum/safe-integer metadata. Removed, unknown, extra, missing, hash-drift, and rehashed semantic drift inputs fail closed.

G3.8A makes the canonical request Value the sole exact caller-claim record and requires Schema 4 to use nullable `verified_*` Release/Retention/compatibility/pointer bindings only after each preflight succeeds. It requires a header-bound canonical terminal result Value and terminal Invocation composite identity, explicit terminal disposition `applied | failed | conflict`, applied-only receipt, typed duplicate references, nullable verified Event identities, and conflict/replay event types. G1.6 must rebuild the three Activation relations, remove insert-time observation rejection, add terminal-result lookup, and preserve G1.5 Release lifecycle, active-pointer CAS, and active/draining Retention protection. Schema 3 construction upgrade is allowed only with empty Activation/active-pointer relations; otherwise it fails closed. This pack is not G1.6 DDL/Manifest/Store implementation and is not a G3.9 request/receipt/result Contract Pack. It adds no Activation DML, active-pointer mutation, production loader, GC/delete, Artifact build/install, or G4-G9 surface.

G3.9 implements the independent Feature Release Activation vertical slice on current Database Schema `5`; its Activation behavior remains the frozen Schema 4/G1.6 semantics and consumes only the updated exact G1/G3 bindings. The current machine authority is `icarus.workflow-contract-pack-g3-feature-release-activation@1.0.0`, `sha256:eb3d316be520e68fe53b6be826046d3f8fa1cf97db298d5703886d1dcb97b70f`; it still publishes the same separate closed request, receipt, and result schemas, domain-separator catalog, and isolated 9 positive / 53 negative / 17 fault fixture artifacts. The request Value is the sole exact caller-claim authority for caller key, actor/session/time, Feature, target/previous Release, expected pointer, exact G3.6 input, target/previous Retention, and exact schema bindings. `authoring/feature-release-activation.ts` owns only `workflow_feature_release_activation_commands`, `workflow_feature_release_activation_invocations`, and `workflow_feature_release_activation_events`; it does not reuse or extend Workflow Runtime, Capacity Admin, or Publisher command unions.

Activation strictly parses canonical request bytes, validates the fixed precedence, and composes G3.6 for all G3.3/G3.5 semantics. One short `BEGIN IMMEDIATE` fills nullable verified facts monotonically, verifies exact Release owner/resource/lifecycle and held Retention identity/status/row-version, checks the observed pointer, transitions previous `active -> draining` when present, target `staged -> active`, and pointer `absent -> 1` or `N -> N+1`, then commits canonical Values, result/receipt, adjacent immutable Invocation/Event chains, and the terminal header. Receipt exists only for the committed applied transition. Compatibility/lifecycle/resource/Retention rejection terminalizes `failed`; pointer mismatch terminalizes `conflict`; both have null receipt and zero Release/pointer mutation. Exact terminal replay appends `duplicate`, same-key domain drift always appends `conflict`, and clean pending recovery reruns exact preflight only after strict command/Value/chain validation. Commit-after-response-loss recovery reopens the real database, appends recovery duplicate events, and never replays pointer DML. Any command/result/receipt/Invocation/Event schema, hash, or binding tamper fails closed without appending audit.

Use `./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.9:generate`, `./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.9:check`, and `./scripts/runtime-toolchain.sh exec -- npm run test:g3.9`. G3.9 is `DONE`; the required independent G3 whole-gate regression has passed and G3 is now `DONE`. The regression added fail-closed verification of the exact per-Invocation Event profile after proving that a schema-valid Event suffix with a recomputed hash could otherwise be accepted. G3.9 now has 34 tests, while its frozen pack, 9/53/17 fixtures, closed schema resources, and all upstream identities remain byte-stable.

G4 adds the isolated, explicitly selected test-only bootstrap and no Runtime business behavior. Its historical downstream-safe/T6d identities remain construction provenance. The affected-chain regression current pack/profile/implementation identities are `sha256:4d2ca7bc3095ae5ac208da8918eabc6d3b7c62e9fab029439f6b01c275f7b8cd` / `sha256:71e0aba29c1ec34cd4d9fa0515409576bdc8fd7f90b9c258bbe0a614b3f0fa3d` / `sha256:28a920856d26325ec14c976a9e1da668c1302e06fd3e2e05abb890dff830f018`; implementation artifact is `sha256:74803b081571df002b900dc6ed23b9c7fc83f86bcb8f096a331cf2011805825e`. The regression fixes the profile's stale G3.9 pin so it now exact-binds `sha256:eb3d316be520e68fe53b6be826046d3f8fa1cf97db298d5703886d1dcb97b70f`. Isolation boundary v2 remains byte-identical at `sha256:883f8f2d4040fd03ea5a49bd38826ce6c37d5556175219b66f335793087f9188`; the pack rebuild is caused only by that exact upstream identity repair, not by new Runtime behavior. `bootstrap/` contains the closed profile, exact fixture set, exact seven-behavior Fake Adapter profile, Virtual Clock profile, implementation/source inventory, isolation proof, strict invocation/result/profile/receipt schemas, and domain catalog. `conformance/g4-test-bootstrap/` contains 9 positive, 32 negative, and 13 fault cases.

`createG4TestBootstrap` has no default path: callers must supply the exact current profile and fixture refs/hashes plus an explicit instance key and canonical root. Every root is an exclusively created deterministic child of canonical `os.tmpdir()`, is bound by owner marker plus device/inode identity, and owns exactly one `workflow-runtime.db`. Relative/aliased/symlink, pre-existing/nonempty, reused, non-temporary, permission-denied, and Production-colliding roots fail closed. Losing an exclusive-mkdir race never grants cleanup authority over the winning root. Fresh and reopened databases go only through `WorkflowRuntimeConnectionFactory` in `candidate_development` mode, preserving current Schema 5/Profile/PRAGMA/identity gates and writing no Registry, active Release, Workflow, Run, Activation, or other business rows. The isolation receipt binds root and database device/inode plus all G1/G2/G3/toolchain/profile/fixture/clock/implementation identities; reopen rejects database replacement before Factory consumption. Tests prove all 84 current Schema 5 business/Registry/audit tables remain empty, zero active Registry/Release facts, unreachable Feature/API/Automation/Production ingress, no real Adapter/network, and no Production/user-data access.

The only test time authority is Virtual Clock seed `g4-virtual-clock-seed-0001`, initial time `1784764800000`; implicit `Date.now`, wall-clock fallback, real sleep, and rollback are forbidden. Fake Adapter accepts only the seven exact hashed invocation records and deterministically replays `not_applied`, `applied_with_receipt`, `applied_but_receipt_lost`, `still_running`, `unknown`, `cancelled`, and `compensated`; undeclared inputs, operation keys, attempts, outcomes, or response bytes reject. The profile is `not_certified`, is never default-enabled, and every Production build/startup/loader consumption rejects with `test_bootstrap_profile_forbidden`. Isolation v2 exact-binds the four G4-owned non-test bootstrap sources, performs a live TypeScript AST/import-resolution graph check over all current non-test source, scans host configuration across the exact `src/electron/assistant/features/setup/scripts` roots, and exempts only the exact 15 G4 machine-authority configuration paths while treating those paths as authority graph targets. Production, Feature, API, Automation, host, and future G5-G9 source may exist and grow, but none may directly or indirectly reach, reference, select, or default-enable G4 bootstrap/profile authority. An unrelated source addition, including an empty `runtime/graph-runtime.ts`, leaves the isolation identity stable; owned-source drift, undeclared bootstrap siblings, forbidden graph edges, package start/default references, launcher references, imported source-root host configuration, and tsconfig aliases fail closed.

The historical independent G4 and T6d regression byte digests remain recorded in Git and the implementation progress ledger. The current Capability/Outbox affected-chain regression passes two byte-stable aggregate generation/check rounds, G4 33/33, and live isolation with 275 source files / 38 production roots / 9 authority sources / 4 bootstrap sources / 0 violations. G2/G3/G4 are `DONE`; G5 Basic Runtime is `READY` but has no implementation and is not `DONE`. No G5-G9 business implementation, T0-T8, Capacity Gateway/Publisher/Watcher, Reconciler, Scheduler, Runtime Command, Projection, Production loader, or Production ingress is present.

Use the managed toolchain for both commands:

```bash
./scripts/runtime-toolchain.sh exec -- npm run contracts:generate
./scripts/runtime-toolchain.sh exec -- npm run contracts:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:archive:check
./scripts/runtime-toolchain.sh exec -- npm run test:g0
./scripts/runtime-toolchain.sh exec -- npm run test:g2:contract
./scripts/runtime-toolchain.sh exec -- npm run test:g2:working
./scripts/runtime-toolchain.sh exec -- npm run prepare-rc
./scripts/runtime-toolchain.sh exec -- npm run prepare-rc:check
./scripts/runtime-toolchain.sh exec -- npm run test:g2:prepare-rc
./scripts/runtime-toolchain.sh exec -- npm run golden:current:semantic-review:check
./scripts/runtime-toolchain.sh exec -- npm run golden:current:seal:check
./scripts/runtime-toolchain.sh exec -- npm run g2:replay-repair:semantic-review:check
./scripts/runtime-toolchain.sh exec -- npm run g2:replay-repair:seal:check
./scripts/runtime-toolchain.sh exec -- npm run golden:current:replay:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:g3:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.query:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.6:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.7:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.8a:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.2a:check
./scripts/runtime-toolchain.sh exec -- npm run contracts:g3.2:check
./scripts/runtime-toolchain.sh exec -- npm run test:g3
./scripts/runtime-toolchain.sh exec -- npm run contracts:g4:generate
./scripts/runtime-toolchain.sh exec -- npm run contracts:g4:check
./scripts/runtime-toolchain.sh exec -- npm run test:g4
```

`contracts:generate` regenerates the current G2 Working Contract/input/candidate/review bundle and current Schema artifacts, then regenerates the dependent G4 test-only pack; it never prepares an RC, creates an approval, or updates sealed expected bytes. G3 artifacts retain separate `contracts:g3:generate`, `contracts:g3.registry:generate`, `contracts:g3.query:generate`, `contracts:g3.6:generate`, `contracts:g3.7:generate`, `contracts:g3.8a:generate`, `contracts:g3.2a:generate`, and `contracts:g3.2:generate` entries. `contracts:check` validates the unchanged predecessor Working/RC/Draft/report/review/seal identities, the additive successor Draft/report/GoldenSemanticReview/seal hash chain, all deterministic G3 packs, current Schema/Store, and the exact G4 source/profile/fixture/isolation identities. After seal, construction-era G0.10, Working, and RC roots are checked from their frozen artifact inventories instead of re-running obsolete "sealed must be empty" assertions. `golden:current:replay:check` is the separate Production Compiler gate for the approved successor and requires 40/40 exact equality.

`g2-production-compiler-replay-repair-v2` is the additive audit successor for the Production Compiler 3.0.1 / Canonical Normalizer 2.0.1 / Proof Algorithm 2.0.1 identity cascade. It preserves every predecessor Working, RC, Draft, review, GoldenSemanticReview, and sealed identity; binds new per-case snapshots and actual results; freezes a complete deterministic Draft; and emits a read-only 40/40 comparison report. The owner approved exact Draft `sha256:29fdd70ea872f9d4e52d49fbd988fff306d95820989920f5f1ecf2bc87019d2b` with report `sha256:2f9edba7af3715f4d5d64328a9fd1a601505bafd1129cee603e87aacf80d92d7`. The resulting GoldenSemanticReview is `sha256:88c5412d1bd97d52a7f9bf41e17bd1db1e19b4a2e5466b3b25384fbdeb7cac0c`; the sealed bundle is `sha256:d99647d8ca6aabc737a793019335e6770aa111a79be7545c4dec00c6e7af2145`. The approval and seal commands are exact-input, byte-idempotent, fail-closed operations with no `--accept`, snapshot update, Publisher, or G3+ path.

Some Working directories and TypeScript module names still contain the historical `v4` or `draft` token. Those names are construction-path compatibility only; the generated lifecycle fields are authoritative. The separately frozen RC path has no v5/v6 token and does not convert the Working paths into Published resources. No review, human judgment, approval, signature, seal, or `conformance/sealed/` write is performed by either RC command; successor review and seal writes require their dedicated, exact owner-approved commands.

The historical G5/G7 T6e ownership repair remains provenance at authority `sha256:36289416db3c8898d9b50c04c5ad43fc6b74ef53bbd3a3c99f9d5f5b72786fa8`, transaction protocol `sha256:7c55b3eff2f29e5dfcbb057d5ff014697ba2e9a421287afa19ec850540cce5f0`, and command protocol `sha256:b12b07b29e9335593c969033c133d221b244798fc079db5fb398b23fbae10789`. Its independent regression is `PASS/DONE`: G5 may create Operational Blockers but cannot resolve/abandon one or implement T6e, while G7 owns T6e authorization, source-specific remediation, Runtime Command Gateway and Command/Invocation/Event audit. Those identities are no longer current after reopening the Contract for the subsequent T6d blocker.

The current construction authority at `governance/workflow-runtime-gate-ownership@1.json` is `sha256:6ba952bf7761f249fa16c0c44a37d48a67c9f5f21c3667a5c966ac59e8affb38`, with affected tree `sha256:9e290b8a9fe583942b6389f294366a698b606cf8e3ddd827cd27588b83b98323` and the preserved generation-time status `T6D_OWNERSHIP_EXIT_CANDIDATE_PENDING_INDEPENDENT_REGRESSION`. It binds unchanged transaction protocol `sha256:3d5474096d89fbd723e34e0d2f9d1dadd1b955b5fc36ff447d026257852cac79` and command protocol `sha256:43cc8ef247fcba4bac5e9fccdd654fd393928120755a6405bad232043f0c94ba`, plus the current Capacity/G1 and declared affected G3 roots. G4 remains a separate isolation authority and is verified separately by the same affected-chain regression. T6d remains `attempt_watchdog_and_retry_timers`: G5 owns only automatic attempt dispatch/execution watchdog, execution-retry and quality-revision timers, plus the retry-schedule consumption primitive. Workflow deadline Command/key/late-result branches, Runtime Command/Invocation audit writes, and manual authorization bypass remain forbidden in T6d.

Workflow deadline enforcement remains wholly G7-owned. The Deadline Watchdog uses the existing `system:deadline-watchdog` System Grant, `deadline_enforced | safety_enforced`, `due_target`, `cancel_workflow_only`, and stable key `workflow-deadline:<workflow_id>:<deadline_at_ms>` to submit the existing `cancel_workflow -> T7c` path through the Runtime Command Gateway. T7c requires `authorized_cancel_command`, atomically records `command_invocation_audit`, returns the canonical result for duplicate submission with a new Invocation, and audits a late request without changing the winning close. `advance_retry_schedule` still maps to T6d, but only after the future G7 Gateway completes authorization, policy/state guard, and Command/Invocation audit before calling the G5 primitive. G5 tests may prove the automatic path and rejection without G7 authorization; they may not forge a Gateway.

`contracts:gate-ownership:generate`, `contracts:gate-ownership:check`, `test:gate-ownership`, and `test:g5:readiness` execute the checked-in 4 positive / 40 negative oracle and bind source/generated agreement, closed fields, raw bytes/tree digest, exact G5/G7 semantics, stable deadline key/System Grant/audit, manual handoff, query/safety ownership, and current Schema 5/T6e constraints. Historical Schema 4 remains byte-identical; current Schema 5 adds no deadline handoff relation and still does not allow G5 to write Runtime Command rows. This repair contains no G5 Runtime module, business DML, partial skeleton, Gateway, T6e implementation, G6-G9, or Production surface.

The checked-in authority retains its generation-time `implementation_authorized=false`, pending status, and next-required-gate bytes; the earlier T6d ownership regression remains historical `PASS/DONE`. The Capacity/G1 exact-binding reopen and the Capability/Outbox execution-binding affected chain have both passed independent regression. G5 is therefore `READY` but has no Runtime implementation and is not `DONE`; G6-G9 remain `NOT_READY`.

The Capacity repair closes the former persistence contradiction with an immutable `prepared` CAP1 Invocation. It is inserted with the pending head and prepared Event in one `BEGIN IMMEDIATE`, has `decided_at_ms` set and `applied_at_ms=null`, and is never updated. CAP4 alone appends `watcher_published` and finalizes the Command canonical result; an exact retry after CAP4 appends a distinct `duplicate` Invocation. Denied/conflict/failed/duplicate, unauthenticated/unauthorized, later invocation numbers, request drift, unfinished duplicate, and new Schema 5 `applied` combinations fail closed.

`contracts:capacity-repair:generate/check`, `test:g1.capacity`, `test:g5:blocker`, and `test:g5:readiness` execute the repaired CAP lifecycle and Schema 5 fresh/upgrade/constraint/Store evidence. Independent regression additionally proves two complete aggregate generation/check rounds, all Schema 4-valid history preservation, Schema 3-to-5 continuity, rollback, G0-G4, ownership, and protected provenance. Current Capacity root/logical artifact/delta are `sha256:d436710893239f01e53d668c23d5ddcfe1a7e4dbee3c00074bc4cd43871c98a6` / `sha256:b15daf99f68f8447aff1da5a9411460497ae29e7067a3802ac588d790066fe30` / `sha256:ca81abe11e332890bde7420fdf8f040856e8076bba9bbc4a03d15ffedb439e3a`; the protocol semantic artifact remains `sha256:0e06b38b98bfd2193dbad24297e75b1422fdcc8f11e297835c87d4baf03aea9b`. The regression contains no G5 Contract Pack, Runtime source, Capacity production implementation, business DML, Runtime Command Gateway, T6e, G6-G9, or Production surface. The execution-binding authority below resolves the former specification blocker, and its independent affected-chain regression now makes G5 `READY`.

The superseded audit recorded G5 as `BLOCKED_BY_SPEC` at the Capability-to-Outbox execution binding. `WorkflowGraphCapability` and both current Compiled Plan schemas contain no `adapter_ref` or `delivery_policy_ref`; the sealed G2 `capability_binding` key set also omits them. The Outbox contract simultaneously requires every Capability producer to freeze an exact Adapter and finite versioned Delivery Policy, while Schema 5 requires non-null Registry-FK-backed adapter, delivery-policy, and policy-snapshot identities on `workflow_outbox`. Therefore T5 cannot be implemented from current immutable machine inputs without a latest lookup or test fixture injection, both forbidden. Resolving this requires an explicit source/snapshot/Compiled Plan execution binding and a governed rebuild of affected Compiler/Golden/G2 seal and G3 Publish closure evidence. No partial G5 Runtime, custom Plan format, mock-only implementation, or G5 identity is checked in.

The historical independent regression evidence was: Capacity fixtures 9/31/14; G1 Capacity 56/56, G1.1 23/23, G1.2 23/23; G0 109/109; G2 47/47 plus 40/40 exact sealed replay; G3.9 34/34 and G3 97/97; G4 33/33; ownership 9/9; blocker 5/5; readiness 6/6. The two aggregate JSON/SQL tree digests were both `dd6c61aa82b29c5edf8d9fc9266727f28ddbd5fff31766dc926f766cf2ac1b6a`; the two G4 14-member raw-byte digests were both `a649e0a1cdc60536a4ea30e08f722fb539f6c9daf71925070604a951953b1606`; live isolation was 278 source files / 38 production roots / 9 authority sources / 4 bootstrap sources / 0 violations. Historical Schema 4 migration/SQLite identity, Schema 3-to-4 upgrade, sealed G2 v2 bundle, and G3.8A remain `sha256:4a8ddeb1f9715399ad96c3bc32efa5e8032a3bd484eaed0159c6a24620c1be43` / `sha256:e46f58e49b42ad53e3d744de86b6d8fb6299236258459c35d9ca3affa440932c` / `sha256:5ac263fe3279c61f74ba6314f5df98fff59a8f8b32acfa784d2040421ebaa3cf` / `sha256:d99647d8ca6aabc737a793019335e6770aa111a79be7545c4dec00c6e7af2145` / `sha256:d8412111a0f3dcabb4ce416b99086701ea3e3911ff431b5457eb957b2f69722f`.

The current execution-binding repair authority is `conformance/capability-outbox-execution-binding/`, root `sha256:48f63ae7be30c61f056f78f713591f34431336cc722d6928fbc6e2783a062088`. Published Capability now owns an exact `outbox_effect`; Compiler 3.0.2 resolves only Published exact Adapter and finite Delivery Policy resources from the pinned snapshot, intersects attempts/duration/timeout/backoff with pinned Runtime Safety, and lowers the immutable `outbox_execution_binding` into Compiled Plan v2. The current toolchain/build are `sha256:90bc7c99cacaf58217dd6d07781788c844385d3c70644c4086d6c997312f60a1` / `sha256:698af607955463f01a404d626586420f3dd8f7a208da87c1e138075b1518ba05`; Plan/result schemas are `sha256:f5bc0a43d5723096295b9a6fcd5a0965c3b98ca810ae8b6dc1a7072996608e06` / `sha256:021da33556e677984b767f99b12800be8454c7516221ec63bb16e1cb26f867f7`. The owner-approved G2 v3 Draft/report/review/seal are `sha256:b8ca7c91839b88b5591daf19f17a30e70b85e441d9dd4905807ef57bc37f7591` / `sha256:5b3b5c721e6cda298da468566eb97da3423fcb86595de4f46dc66f79ebb55e99` / `sha256:ceddcefcab8ff41a5e9b5d2ceb89dabcd3f9199639bb247d132a7c79c33dc15b` / `sha256:b3ed9e43bd0fadaf40520257926dcf690ee8495bb417220245f248385bde9efb`; the sealed artifact hash is `sha256:967437bb9f91e32e5014b2af90a23f5646e491eb427bdf55accb345ead70db8f`, with current replay 40/40 exact.

Schema 5 is unchanged. The handoff Contract requires T5 to exact-query Registry rows by ref/hash/publication state, persist the effective Policy snapshot as an immutable `workflow_values` row, and write its ID/hash with Adapter/Policy Registry row IDs/hashes into the existing six deferred-FK columns. Missing, mismatched, unpublished, moving/latest, non-finite, hash-drifted, and test-only-to-Production bindings fail closed. G4 fixture authority, runtime latest lookup, custom Plan formats, G5 Runtime DML, Capacity production runtime, Gateway, T6e, deadlines, G6+, and Production activation remain absent.

This repair and its independent affected-chain regression are `PASS/DONE`. G2/G3/G4 are restored to `DONE`; G5 is `READY` but not implemented and not `DONE`; G6-G9 remain `NOT_READY`. The next task is only the full G5 Basic Runtime Gate implementation.

G5 Basic Runtime is now `G5_REPAIR_EXIT_CANDIDATE_PENDING_INDEPENDENT_G5_WHOLE_GATE_REGRESSION`, not `DONE`. The second repaired closed `icarus.workflow-contract-pack-g5-basic-runtime@1.0.0` is `sha256:7e89802e0de82baf3149f85f448e749e6df96041089d5519b0217e43592e37b2`, with member tree `sha256:66e23693e10867d77c1f4edf98d03f658e5a52be11a40bf6d87af19cc8bc69e2`, protocol `sha256:87543c7cd905d47fa493f9558ea39b5be54ff0aafc5927c9237423de9f6c6ee3`, typed-record schema `sha256:a3f356b74a7db2f9b6d01fa6df259be08d3206e167b697eb669911940b8bf3d1`, implementation identity `sha256:5d14d68e983809c731f5483b013c455a306c7d1154e2f66e9861c6663bff537c`, and 17-source tree `sha256:e832c84114c5481375523c92b4b9132164e3e101539b9942af26e792dc48a9bc`. Its deterministic generator/checker binds the unchanged ownership `sha256:6ba952bf7761f249fa16c0c44a37d48a67c9f5f21c3667a5c966ac59e8affb38`, execution binding `sha256:48f63ae7be30c61f056f78f713591f34431336cc722d6928fbc6e2783a062088`, Capacity, Schema 5, and sealed G2 identities; the 9 positive / 14 negative / 16 fault fixtures remain closed and cover every `T0/T0p/T1/T2a/T2b/T3a/T3b/T4/T5/T6a/T6b/T6c/T6d` rollback boundary plus Capacity crash/reopen/replay.

The repair makes persisted Run/Scope/Plan/node bytes the sole runtime authority. T2a requires the exact Published Definition Plan pin and T2b materializes exact capability resource IDs/hashes plus normalized node bytes. T3 now evaluates status/code/child-exit routes, condition/default/all/first/no-match groups, Strong Kleene root/all/any/quorum/expression triggers with immutable witness cuts, required/optional/default/single/list input sealing, and per-post-state early eligibility. Settled completion verifies policy/rule/fact/selector hashes, evaluates all rules with priority fall-through, supports exits and terminal-node IDs with all three pick modes, fences normal closes, and creates same-transaction `engine_error/no_exit_selected` when none applies. T4 derives execution context, wait Contract, correlation, timeout, terminal named exit, and structural output from materialized authority; T5 no longer accepts caller binding/context and validates exact Published Adapter, finite Policy, Runtime Safety, and the six Schema 5 deferred-FK bindings in its transaction. T6b checks Plan node type, persisted dispatch identity, and current fences; T6c requires the exact Plan wait binding and distinct typed two-phase authorization Values; T6a/watchdog derive retries only from the Plan, while T6d re-enters live Capacity admission and persists revision/change/config lineage. All business writes remain short synchronous `BEGIN IMMEDIATE`; external work remains outside transactions.

Direct G5 generation is deterministic across two complete rounds; the selected generated-file manifest digest is `f6ccc276b78481a75fa473c920902d50c95f2a270474d6ebbab579e763b520a1`. `test:g5` includes fixed rollback/reopen/replay paths, route/trigger/input/completion SQLite matrices, and a 12-run fast-check differential against an independent Strong Kleene model that compares target terminal/ready rows, Fact/Event keys, facts ledger consumption, and blocker state. The earlier execution differential continues to verify attempt terminal rows, Fact/Event single-write, active-slot ledger release, and blocker state. The frozen G0.10 generated identity is unchanged; `contracts:capacity-repair:check` uses an additive current compatibility checker that byte-checks the historical addendum while admitting only the current sealed v3 execution-binding directory. G6-G9 remain `NOT_READY`; the only next task is a fresh independent G5 whole-gate regression, and G6 must not start before it passes.

The third repaired closed G5 pack is `sha256:73db71fbfe4ffbb41fc40181eb94d9d6f3fdbcff19a1f1e2180e427fdab6f348`, with member tree `sha256:17bdfbac49c858084cc1b47f60eb56dc096a4b6289a7050cc2fdb2bf55d57b26`, implementation `sha256:867643cf2dfdb3e25b72d50023248a0aa8d49b388ff83e94236d6286b98ec842`, and 17-source tree `sha256:7eb638cbe84c65b38511b9e699eab62f4b08ca2801e519498d052287156555c7`. The unchanged protocol, typed-record schema, ownership, and execution-binding identities remain `sha256:87543c7cd905d47fa493f9558ea39b5be54ff0aafc5927c9237423de9f6c6ee3`, `sha256:a3f356b74a7db2f9b6d01fa6df259be08d3206e167b697eb669911940b8bf3d1`, `sha256:6ba952bf7761f249fa16c0c44a37d48a67c9f5f21c3667a5c966ac59e8affb38`, and `sha256:48f63ae7be30c61f056f78f713591f34431336cc722d6928fbc6e2783a062088`.

T3 data resolution now freezes the selected immutable Value rather than a multi-port node-output or scope-input envelope. A direct port reuses its hash-verified Value ref; pointer, literal, embedded, default, and list aggregation values receive deterministic Plan-bound identities and canonical persisted bytes. Input sealing resolves the exact Published Registry schemas carried by the persisted compiled node, validates single/default values, list items, and the sealed list array, and enforces port, item, aggregate, and Runtime Safety byte ceilings. Initialization pointer, data-authority, trigger, or input failures commit the same-transaction `engine_error/fixed_point_resolution_error` close request and Run/Scope fences; injected pre-commit faults still roll back completely and replay exactly.

The third-candidate G5 suite has 64 tests across four files. Fixed SQLite cases cover direct and pointer selection from both scope and node envelopes, all single/list schema and size failure classes, initialization error commit, reopen/replay, and pre-commit rollback. A separate bounded model that imports neither Ajv nor runtime helpers drives a 12-run fast-check differential through production Schema 5 SQLite and compares ready/engine-error rows, Fact/Event keys, facts ledger consumption, and blocker state. That evidence remains historical candidate evidence and does not close the fourth-review blocker below.

The fourth targeted applicability review marks current G5 `BLOCKED_BY_SPEC`, not `DONE`. The current Plan schema admits a generated schema with either `schema_json` or an unconstrained non-empty `schema_ref`, but no current Spec, Registry, Store, or G5 Contract defines the latter's resolution namespace or maps a Plan-local generated schema to the non-null Published Registry schema pair required by Schema 5 `workflow_values`. G5 cannot mint that Registry authority or persist a derived Value with a null schema resource.

The same review proves that current Compiler 3.0.2 does not lower Source join `expose` into typed Plan `output_ports`: a join with `source_value -> expose.renamed` compiles with `output_ports={}`, and a downstream edge from `join.renamed` deterministically rejects as `schema_not_assignable`. Runtime derivation would manufacture a compiled contract and contract hash absent from the sealed Plan, while the current structural scheduler publishes the input snapshot instead of a canonical `NodeOutputEnvelope`. Resolution requires an additive generated-schema persistence authority plus a governed Compiler/Plan/Golden/G2 successor for join output lowering, followed by affected G3/G5 identity and regression rebuilds. Frozen G2 trees and current Schema 5 remain unchanged; G6-G9 remain `NOT_READY`.

The current R-019 repair supersedes only the current labels in the historical paragraphs above; it does not rewrite their bytes or evidence, and R-018 remains uniquely Feature Release Activation persistence. `conformance/generated-schema-join-authority-repair/` is the closed authority; its exact R-019 decision/pack identities are `sha256:b53f8a77e51adba5d1b8f41766336bae2a59a05e5ced1959996218d745d5ebd4` / `sha256:0c9b1a04a013bf6284b36c550ead6d81cffecacbe4a9d6495d6153b2335a04fc`. A generated descriptor now requires equivalent JCS `schema_json + icarus-generated-schema:sha256:<raw-hash>` plus raw/domain/parameter hashes and byte length. Schema 6 persists exact content in `workflow_generated_schema_contents`, binds it to a sealed Plan in `workflow_plan_generated_schemas`, and gives `workflow_values` a strict mutually exclusive `registry | plan_generated` authority. Frozen Schema 5 remains at `migration/workflow-runtime-schema-v1.sql`, raw hash `sha256:2ead40dc2f1618f87247e9d3bb476266797c38560e1ad0537a6afa6f71a3fbf6`, `user_version=5`, SQLite identity `sha256:5ee3c119cc6a0e0552e2a6fe45b51c8ffd08ec7acdbac66748978ed0d21fdb0a`; fresh Schema 6 uses the additive `migration/workflow-runtime-schema-v6.sql`, raw hash `sha256:16a46e84c77d734013e18b4b00b86564f6188ea73717763e9fb7a884d62faa41`, `user_version=6`, and is the current dependency-manifest/Store bootstrap authority. G1 root/dependency/schema/manifest are `sha256:3cc206a6dfb1bbaed1bb0f4305323729db23d839652d8a0e020a9a6c4d3e3dd6` / `sha256:0e4e38f3e31e53bcc9687f1928c44c4c57dc1c443fff774a3107ba057193120e` / `sha256:37f0102a9d6b0077f0d44f20182a7d5768ce32b1c0c2c3998937178b06c9b474` / `sha256:30b88b9df7dc7f8318ce8fcb5c38ca94c585d8585deff83235b8a8b8f582e0e2`.

Compiler 3.0.3 owns join `output_ports`: it lowers each ASCII-ordered expose from the corresponding compiled input contract, carries rename/single/list/default/max/required rules into the `join_expose` parameter hash, and proves downstream assignability. Caller output authority is rejected. Toolchain/build/proof/Plan/result schema identities are `sha256:acc4bd6e6444f9903e3054bf637dee28ee8b25fbfc5eebd49b9aaf37582ff493` / `sha256:eae5b2002226b9110dddba57dce528af91439ae760c5d30636631bcb464d86b2` / `sha256:b6fda13a0acddf052cae5ed6f1bc89f2b9cfa91affbfcbb80aa44365f78c35d9` / `sha256:e582abc7a221f4d1afd66d12c2a87816cb228f6139a77d9abfaa1a397844f947` / `sha256:ee41b9dff7eb2c97a75c81a7c15ec3bcf935ce233c29468a4ef7b7bfa047987e`. G2 v4 remains byte-identical and independently replays 40/40. The additive current v5 binds v4 as its unique predecessor; RC/Draft/report/semantic review/semantic artifact/seal internal/seal artifact are `sha256:6fe9c6804237ea00dfb73c50dcd9a5dd658956b54e67db554817067433c584f6` / `sha256:965b1af2c4688c827a9d63f6b939a130271ab0e8b0fcf85a614cdd2620cb757e` / `sha256:96f6d0feee0e4a7d77349e0d8210875dc518a40ada167785c0859193a0246180` / `sha256:4481515b905ca062e3d028e17bb13ed2d4080059e844efa6c609ef292967e0de` / `sha256:f5c07ae45d93124cda1247aeca1cdb4df8d2cc7398e2129cd7d90f4ab529526b` / `sha256:b37ddf415d12d759ddd4b72b754568e01715704d254da26e3355e0898cfeda05` / `sha256:f59040be6f71d8655afcb11ab4527a6683125a7a4e683f1e734b44448f7bb72e`; current replay is 40/40 exact with 95 assertions.

The rebuilt current Publish closure is G3.1/G3.3/G3.5/G3.6/G3.7/G3.9 `sha256:c9364171a3d28a752d4510f59e5e45016cd86be14d7e151b483fc0c6c7a2807d` / `sha256:590acdd52626838bf30ae14bb04b6f0ee59a95e1efadc4911ff850acc2970763` / `sha256:b6a4ec1dac738c6036869c763708f8aa144d0a864c8c392ee18cb3aba8c83417` / `sha256:7f807ae53e13bcec7712f77c1ebaba7aab5f72d2779ed5d99d33b0e6c54e98d3` / `sha256:5d023a5323aec482781b0e992197571db9a09481a394eaf955d4598c249e4ec1` / `sha256:cbb6b355819b1eefefa7af5289b10b367c42bbe32b09f003151bc7f9ebf475d7`; frozen G3.8A remains `sha256:d8412111a0f3dcabb4ce416b99086701ea3e3911ff431b5457eb957b2f69722f`. G4 pack/profile/implementation are `sha256:bd7b944c66181e05add3618e6355a1acc64ff452dc4c027d4556c776a4402046` / `sha256:87956027bca69d9fcdb1891298dc2a083a9e413c4530d7aff71473b74a58c106` / `sha256:1a04adf90718c7ad7f53caf93fd2a02ca00857e86ab96bfcf81ae137aef1a552`; the implementation artifact is `sha256:4ced1df4314038474b41c19b313531b25bc31671969529caa33876f18d448275`. Ownership is `sha256:712a7440e83f087e4bbb1e465a1a677a16708429f46766029baa0f90734e5017`; the affected tree is `sha256:3d68cf3c084bee62077d5561545919a9abfc9d0ba6f3d649bb4b8ffd52cd827a`; Capacity stays `sha256:d436710893239f01e53d668c23d5ddcfe1a7e4dbee3c00074bc4cd43871c98a6`.

Two managed aggregate generate/check rounds are byte-identical; the pre-generation and both post-round Contract+Schema JSON/SQL tree digests are `4d241e95c63bedf121d960c2c674af632f594eb85111a178662f02222ca20481`. R-019 authority, G1.1/G1.2/Capacity, G2 including real v4/v5 40/40 replay, G3, G4, ownership, readiness, blocker, whole G0, typecheck, build, final Core bind, and post-build G0/G3/Store/readiness/blocker all pass. Frozen G0.10, G2 v1-v3, G3.8A, and Schema 5 are zero-diff from `0a48f60`; G2 v4 is zero-diff from `1a7cf69`.

The independent affected-chain whole-gate regression is `PASS`; the current state is exactly `GENERATED_SCHEMA_JOIN_AUTHORITY_REPAIR_DONE`. R-019 is `CLOSED/DONE`, and current G1-G4 are restored to `DONE`. G5 is only `READY_FOR_G5_REPAIR`, not a repair candidate and not `DONE`; the historical G5 basic-runtime pack remains excluded from active aggregate generation/check and cannot be promoted as current authority. G6-G9 remain `NOT_READY`. The only next construction task is a new G5 repair that consumes current R-019, Schema 6, G2 v5, G3, and G4 authority; it must not start G6, certification, or Production activation.

The current G5 construction authority is now the separate `contract-pack-g5-basic-runtime-repair.json`; the historical `contract-pack-g5-basic-runtime.json`, its fixtures, and its reference artifacts remain read-only archive inputs and are not active candidates. The repair consumes exact persisted sealed Plan bytes, R-019 generated content and Plan-binding rows, Schema 6 `plan_generated` Value identity, G2 v5 compiler-owned join outputs, the rebuilt G3 closure, G4 bootstrap, ownership, and Capacity. T4 publishes a canonical `NodeOutputEnvelope`; each present join-exposed port is a distinct immutable `plan_generated` Value, optional absence is explicit, and downstream resolution follows the port ref/hash/schema tuple rather than the input snapshot. The current status is `G5_REPAIR_EXIT_CANDIDATE_PENDING_INDEPENDENT_G5_WHOLE_GATE_REGRESSION`: G5 is not `DONE`, G6-G9 remain `NOT_READY`, and the only next task is a fresh independent G5 whole-gate regression.

The current NodeOutputEnvelope schema-authority repair supersedes only the current labels in the historical paragraphs above. R-019 is `CLOSED/EXIT_CANDIDATE_PENDING_INDEPENDENT_AFFECTED_CHAIN_REGRESSION`: every Plan node has one closed Draft 2020-12 `output_envelope_schema`, Schema 7 admits the first-class `node_output_envelope` Plan-generated Stored Value tuple, and the dedicated Store boundary verifies exact Plan/schema/content/hash/length/ownership/provenance on write, read, replay, reopen, and recovery. G2 v6 now exact-binds `node-output-envelope-golden-authoring.ts` at raw `sha256:a574091ca544a7b838936403d1bd55d3f1757468d453d5205aaa6f8a11f897ec`; the checker uses TypeScript local-symbol identity, accepts legal named-import aliases and split imports from that exact module, and rejects duplicate, conflicting, shadowed, indirect, or drifted bindings plus exact-module dynamic import options/attributes and direct require variants at every legal argument count. The old v5 authoring generator cannot satisfy the v6 manifest or machine checker. R-019/G1/G2 v6/G3/G4/readiness identities are `sha256:7a852ff21a77a767b708ab8a4fc5c329024ca954422b26d71210b0385ce05441` / `sha256:b60e3c7fe91d1cfab341d487102c7bff13ad73a320444b45fb6ea71d8b914306` / `sha256:0820328ae1cfdba7d05948d9e36498a5428d997d6eabfb833ef0ba7d84b77db7` / G3.1 `sha256:54355b3c74eb311e495ea31effcbfca6e3ce7547f2ccae663805556060b0b685`, G3.3 `sha256:746280b172ab970a953a20aaaf3dbff557fa7aaecfad6e20bcedc0a0171d72cb`, G3.5 `sha256:0ef337e5b94dcbd279589a7522744462e7a5240e12be54cd47f6afd413675ed1`, G3.6 `sha256:207c7604cf8157dc6e17fe4440bdb6651fed22018e094d0a4342e4dce3c1117d`, G3.7 `sha256:d25e7842961ee76b5736b3217628daf5adf7cd00b52d64c15020b7a2bde3f622`, G3.9 `sha256:5411955aa8cd10888fb1ca3df38f311d0a0310d2bd5570ef1f7a9ed41fe08d95` / `sha256:2b27a8fad1e9a690922186d11bc173f4242174efc843c3eb35e8dfeb94f5c34f` / `sha256:df8fddc636ac9e4eb0835a26e88c47f7565969f04e95ac33b59d7f16bc5cd2e8`.

Current G1-G4 closure remains reopened pending a new independent affected-chain regression. Historical Schema 5/6, G2 v1-v5, G3.8A, G4 bootstrap, ownership authority, lockfiles, and invalidated G5 candidate bytes remain frozen. G5 is `BLOCKED_BY_SPEC/NOT_READY` because its untouched Runtime still assigns the first business-port or carrier authority to the envelope object; it must be rebuilt only by a later independent G5 successor that consumes this Store boundary. G6-G9 remain `NOT_READY`; Registry latest/fabrication, network/fallback, Gateway/audit/T6e, blocker resolution/abandon, workflow deadline, manual bypass, certification, and Production remain forbidden.

The independent NodeOutputEnvelope affected-chain regression has since closed R-019 and restored current G1-G4 to `DONE`; the paragraphs above remain historical provenance. The current G5 construction authority is the regenerated `contract-pack-g5-basic-runtime-repair.json`, pack `sha256:545ca1a8550901b4ffba61e2b06888b2537e65faa6caf7a4192ff4938665c677`, member tree `sha256:9db22cbf1caa39205aff758c65b82f8a10192d7f98cc5c69312e5c5a47b13bac`, implementation `sha256:8107311c6ca5c26b1da2410200f94410b0fdc6c9f08323b2d04b356418ffec71`, and 18-source tree `sha256:700921bd70425d5448316444263dcbb08a1fcb409a54b37fd186aac82646c206`. It exact-binds R-019 `sha256:7a852ff21a77a767b708ab8a4fc5c329024ca954422b26d71210b0385ce05441`, Schema 7 `sha256:b60e3c7fe91d1cfab341d487102c7bff13ad73a320444b45fb6ea71d8b914306`, G2 v6 sealed artifact/bundle `sha256:5cf2d899d0bf8d7cc0d4b70cc7796a123b8b5384bbbefe3e204e70bddf33fe11` / `sha256:0820328ae1cfdba7d05948d9e36498a5428d997d6eabfb833ef0ba7d84b77db7`, current G3/G4, frozen ownership, and Capacity. Historical G5 pack, fixtures, reference, implementation, identity, and test claims are not current aggregate authority.

T4, terminal T6a, and durable-wait T6c now share one canonical NodeOutputEnvelope persistence boundary. The envelope always uses the node's exact sealed `output_envelope_schema`; each present member retains its exact business-port schema while the envelope object never borrows that schema. Derived and aggregated Values, the envelope Value, and every present member are verified through Schema 7 Store write, read, exact replay, reopen, and recovery for canonical payload bytes, schema ref/hash, content hash, byte length, Plan/port-set binding, graph-run ownership, provenance, and `row_version=0`. T6a accepts only the exact compiled output-port set on terminal success, T6c publishes the typed `resolution` port, and the reconciler rejects inline, root-property, implicit-result, carrier-schema, and guessed Plan/schema fallbacks.

The current closed fixtures are 21 positive / 28 negative / 17 fault cases across T0-T6d, intake/routing/domain claims, state/context lowering, static graph fixed point, scheduling/ledger, exact execution binding, Capability Effect/Outbox, waits/signals/timers/approval/inbox, automatic retries, Capacity Admin, Operational Blocker create/open/cache, and NodeOutputEnvelope Store recovery. The selected protocol/positive/negative/fault/reference identities are `sha256:bd72e28685e09870bac59c3a543d6180e39208d2ce029ca49bb61ee9fddc38cb` / `sha256:4d3c3847b862c4d41909ff06c36209cd6b59506d31a53ccc64a2f4112c25686c` / `sha256:b5291f72047fdd4e6cc9f7de3767b66c75b9671ad550bb216faac01563262779` / `sha256:0b973b6b2d297df0afbd6b8b3a4f9ce748bda2f6c65333b1b012c64f0e6606d3` / `sha256:d03cabfe7782cf315ffb0c24358ef21c77ef73b959da5a55e24692dcdc307717`; the evidence tree is `sha256:dfa084bda9b48df486cd2a92a51a782923e1b9dbda3ce37a7b5ca5be7176e21a`.

Current status is exactly `G5_REPAIR_EXIT_CANDIDATE_PENDING_INDEPENDENT_G5_WHOLE_GATE_REGRESSION`, never `DONE`. G6-G9 remain `NOT_READY`; Gateway/audit/T6e, blocker resolve/abandon, workflow deadline, manual retry bypass, certification, Production activation, and G6+ remain outside this candidate. The only next action is a fresh independent G5 whole-gate regression against these exact bytes.

The G5 fixture artifacts are executable records rather than case labels. Every positive, negative, and fault row carries a closed category, exact surface, concrete production operation and handler binding, independent behavior parameter, structured input, optional fault boundary, exact oracle, and domain-separated binding hash. `test:g5` executes every checked-in record exactly once against its declared T0-T6d, Capacity Admin, Operational Blocker, or NodeOutputEnvelope Store boundary. Generated-output rows first execute the independent join publication model and then persist its exact result through the Schema 7 Store. Every row compares its declared durable relation plus a canonical digest of all application tables before/after execution and after Store reopen; replay cases must remain byte-stable, rollback cases must leave the whole database unchanged, and intentional conflict/tamper audit rows must commit their exact evidence. Missing, duplicate, unknown, unhandled, multiply handled, repeated, non-checked-in, category/surface/handler drift, operation/behavior/fault drift, and oracle drift fail closed. `contracts:g5:check` proves only closed structure, identities, and byte equality; fixture counts are not runtime-execution evidence.

The static-child-Plan prerequisite repair supersedes only the current status and identity labels above; all sealed G2 v1-v6 and historical G5 bytes remain immutable. Compiler 3.0.5 build/toolchain `sha256:7eb0e64d00e79d3684f401a46549568f8997d2e02a6cffd0b573326dfb08bcc6` / `sha256:a48c43917d27271bf2edfc0b9a8625519e098917f2768ea447ffda7eb0d19c4e` now returns a closed `icarus.workflow-compiler-static-child-plan-bundle/1` beside the parent Plan. Bundle entries carry exact closure key, pinned/inline child source, and independent child Plan in parent-closure order; the bundle never enters parent Plan canonical bytes or acts as a second Golden oracle. Frozen G2 v6 remains 40/40 exact at bundle `sha256:0820328ae1cfdba7d05948d9e36498a5428d997d6eabfb833ef0ba7d84b77db7`.

Production T2a verifies the complete bundle before opening SQLite, then atomically inserts or exact-verifies the parent and every unique static child Plan plus each Plan's generated-schema contents/bindings before CAS-setting the build compiled. It rejects membership/order/key, source/scope/hash, Plan ref/hash/bytes, nested lineage, compiler/toolchain/normalizer/proof/error/safety/catalog, collision, partial binding, live Run/Scope fence, build row-version, and exact lease owner/token/expiry drift. Compiled replay is verify-only and cannot repair missing rows. T2b remains unchanged and rejects Dynamic nodes as G6-owned.

The additive bridge pack/member tree is `sha256:0f4bba85f8d25a16024877a236724dc119393d87aa773ca791253bbbfcd8ee90` / `sha256:c1c292e46a57caf7f190965d10960cf3bc4525669b9ba07e35ce2921e213b02d`, with 4 positive / 12 negative / 6 fault cases and status `DIRECTED_REPAIR_CANDIDATE_PENDING_INDEPENDENT_AFFECTED_CHAIN_REGRESSION`. The rebuilt G5 pack/member tree is `sha256:07a19052c602ca26abd93ac7a8294fd69a0ab087ac0a84e9e73feecdb84108ea` / `sha256:845b9289a36ecc056d28337b94d565cfc26b422907dbb62d6acdacfd42bbb446`; implementation/19-source tree is `sha256:efb0b516d7a27d602ef44ef2743b94f8af6f5e3b596cb4ad33e3924ed76600be` / `sha256:72f81daa7e819d2633597df42da6fc8f6edb1f35944727f8728d4d031de386e2`. Its 21 positive / 28 negative / 17 fault fixtures remain closed. G2 and G5 are reopened and not `DONE`; G6-G9 are `NOT_READY`. Only a fresh independent G2/G5 affected-chain regression may restore closure and G6 readiness.
